# Simplified Fleet configuration for AIDoc deployment
# Define the namespace where your application will be deployed
defaultNamespace: aidoc-system

# Target specific clusters using labels
targetCustomizations:
  - name: raspberry-pi
    clusterSelector:
      matchLabels:
        env: prod
