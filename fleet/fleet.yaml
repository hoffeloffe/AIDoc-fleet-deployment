# Basic Fleet configuration
defaultNamespace: aidoc-system

# Target specific clusters using labels
targetCustomizations:
  - name: aidoc-deployment
    clusterSelector: {}
    forceSyncGeneration: 1
    helm:
      # Use a valid release name pattern
      releaseName: "aidoc-app"
